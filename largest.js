function largestOfThree(a, b, c) {
    return Math.max(a, b, c);
}

// Example usage:
let num1 = 10, num2 = 25, num3 = 15;
console.log(`Largest of ${num1}, ${num2}, ${num3} is: ${largestOfThree(num1, num2, num3)}`);
